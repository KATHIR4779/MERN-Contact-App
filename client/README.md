# Client (React + Vite)

- Copy `.env.example` to `.env` if you need to change `VITE_API_URL`.
- npm install
- npm run dev

The client has two main components: `ContactForm` and `ContactList` and uses `useState` for state management.